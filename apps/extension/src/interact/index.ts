export { enumerateElements, roleOf, sliderFacts, toggleState, ELEMENT_ID_ATTR, MAX_ELEMENTS, MAX_ELEMENTS_BYTES } from './enumerate';
export type { ElementEntry, ElementSnapshot, SliderFacts } from './enumerate';
export { performInteraction, snap, stillFits } from './perform';
export { accessibleName } from './name';
